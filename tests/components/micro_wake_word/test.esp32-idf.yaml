# ESPHome build test: micro_wake_word on ESP32 (ESP-IDF)
substitutions:
  i2s_lrclk_pin: GPIO25
  i2s_bclk_pin: GPIO26
  mic_din_pin: GPIO27

esphome:
  name: test-mww-esp32

esp32:
  board: esp32dev
  framework:
    type: esp-idf
    sdkconfig_options:
      CONFIG_ESP32_SPIRAM_SUPPORT: y

psram:

logger:
  level: DEBUG

<<: !include common.yaml
