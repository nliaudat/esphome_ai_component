#include "value_validator_coordinator.h"

namespace esphome {
namespace ssocr_reader {

// Currently implementation is fully inline in header for simplicity as it's just a wrapper.
// Kept this file if we need to expand later or to satisfy build systems expecting .cpp
// But for now it can be empty or just include the header.

}  // namespace ssocr_reader
}  // namespace esphome
