text_sensor:
  - platform: template
    name: "Crop Zones"  # Exposed to HA
    id: ${id_prefix}_crop_zones_sensor
    #Update ESPHome when changed from HA
    # on_value:
      # then:
        # - lambda: |-
            # ESP_LOGI("crop_zones", "Received crop zones: %s", x.c_str());
            # id(${id_prefix}_crop_zones) = x;  //Store in global var
            # id(${id_prefix}_meter_reader)->set_crop_zones(x);
