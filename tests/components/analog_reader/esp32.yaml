# ESPHome build test: analog_reader on ESP32 (Alpha)
esphome:
  name: test-analog

esp32:
  board: esp32dev
  framework:
    type: esp-idf
    sdkconfig_options:
      CONFIG_ESP32_SPIRAM_SUPPORT: y

psram:

<<: !include common.yaml