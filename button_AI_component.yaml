button:
  - platform: template
    name: "Start Flash Calibration"
    id: start_flash_calibration_btn
    icon: "mdi:timer-sand"
    on_press:
      - lambda: |-
          if (id(meter_reader) != nullptr) {
            id(meter_reader)->start_flash_calibration(); 
          }
